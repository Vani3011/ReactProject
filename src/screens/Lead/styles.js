import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles((theme) => ({
    cardMain: {
        // padding:"20px",
        margin: "25px",
        backgroundColor: "#ebe7e71f",
    },
    leaddetailsContent: {
        padding: "20px",
    },
    tabContent:{
     height:"600px"
    }
}))