import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles((theme) => ({
button:{
    marginTop:'10px',
    width:"290px",
    backgroundColor:"#0072fff5",
    textTransform:"capitalize",
    letterSpacing:"0.2px"
}
}))